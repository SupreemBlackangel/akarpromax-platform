# حالة التنفيذ — تدقيق سبتمبر 2026

جرد بالفحص لا بالافتراض. كل سطر «الدليل» ناتج أمر شُغِّل على الشجرة الحالية
(فرع `refactor/architecture-foundation`، آخر كوميت `e7ca687`)، لا ذاكرة ولا خطة.

**قرار معلن**: مستودع البرنامج المكتبي المذكور في البرمبت
(`E:\akarpromax-office\AkarApp_Next`) **غير موجود على هذا الجهاز**. الشجرة الحيّة
هي `E:\Akarpromax new 2027\AkarApp_SOURCE` — وهي التي بُنيت ودُقِّقت، وقد أكّد
صاحب المشروع هذا الاختيار سابقًا في هذه الجلسة. الجزء F أدناه يخصّها.

---

## الأعداد

| | تم | جزئي | لم يبدأ |
|---|---|---|---|
| A — مراجعة التصميم (7) | 1 | 2 | 4 |
| B — النظام الإعلاني التجاري (4) | 0 | 1 | 3 |
| C — حدّد أرضك (6 بنود) | 6 | 0 | 0 |
| D — إشعارات التطبيق المكتبي (3) | 0 | 2 | 1 |
| E — بروتوكول المكتب، الجزء A (3) | 3 | 0 | 0 |
| F — برنامج المكتب (5+4) | 4 | 0 | 5 |
| **المجموع (29 بندًا)** | **14** | **5** | **10** |

---

## A — مراجعة التصميم العامة

| البند | المرجع | الحالة | الدليل | ما بقي |
|---|---|---|---|---|
| A1 الخطوط والهوية | `app/layout.tsx`، `src/styles/tokens.css` | **تم** | `layout.tsx:4` يستورد `Cairo, IBM_Plex_Sans_Arabic`؛ `tokens.css:135-147` فيه `--font-heading-stack` و`--text-xs..2xl`؛ كوميت `2af088c` | — |
| A1ب الشعار في كل موضع | `src/components/ui/BrandMark.tsx` | **جزئي** | مستعمل في 5 ملفات فقط: `Brand.tsx`، `public-header/footer/sidebar`، `mobile-navigation`. `AuthPageShell.tsx:139` يرسم `<img src="/brand/logo.svg">` مباشرة؛ `app/admin/admin-sidebar.tsx:95` ما زال يرسم `<span>A</span>` | استبدال في `AuthPageShell`، `AccountDialog`، `DashboardPageShell`، `admin-sidebar` |
| A1ج قاعدة ESLint للألوان | `eslint-rules/no-raw-colours.mjs` | **جزئي** | القاعدة موجودة، لكن `eslint-raw-colour-allowlist.json` **كائن** لا مصفوفة وما زال يغطي الشجرة؛ **1614** استعمالًا لألوان Tailwind خام في `app src components` | تفريغ القائمة تدريجيًا (المرحلة 3) |
| A1د EmptyState موحّد | — | **لم يبدأ** | `grep "text-5xl mb-4"` = **16** موضعًا؛ **403** إيموجي في **127** ملف `.ts/.tsx` | مكوّن `EmptyState` واستبدال الإيموجي |
| A1هـ لا نص تحت 12px | — | **لم يبدأ** | `text-[9px|10px|11px]` = **88** موضعًا | رفعها إلى 12–14px بوزن 500–600 |
| A2 سياسة الإعلانات | `src/config/standard-public-ad-registry.ts`، `src/components/ads/ad-slot-frame.tsx` | **جزئي** | **كل** العوائل `heroEnabled: true` (الأسطر 27-45)، بما فيها `about/contact/advertise`؛ الإطار الفارغ يرسم `ad-slot-empty` بوسم مراجعة (`ad-slot-frame.tsx:115-132`) لا السياسة الثلاثية | `heroEnabled:false` للعوائل العشر؛ سياسة مباع/«أعلن هنا»/محجوز؛ انهيار الأعمدة؛ `max-width:1100px`؛ أعمدة sticky |
| A3 الرأس والهيدر والجوال | `src/components/public/` | **لم يبدأ** | لا ملف `mobile-bottom-nav.tsx` في `src/components/public/`؛ `public-footer.tsx` بلا `details/summary`؛ `NewsTicker.tsx` بلا `position/sticky` (ليس sticky — بند مستوفى ضمنًا) | هيدر صف واحد للجوال، شريط سفلي بخمسة عناصر، فوتر accordion، `PageHeader` بحدود 32/26px |
| A4 البطاقات | — | **لم يبدأ** | لا أثر لبنود A4 في الشجرة | بطاقة العقار والمؤسسة كما في الخطة |
| A5 لوحة المكتب | — | **لم يبدأ** | `grep "خطوات البداية"` = صفر | العنوان بلا معرّف، شارتا حالة، بطاقة خطوات البداية، قائمة جانبية 64px |
| A6 الدخول والتسجيل | `src/components/AuthPageShell.tsx` | **لم يبدأ** | الملف موجود لكن بلا تمييز outline/ممتلئ للأزرار الاجتماعية | أزرار Google/Facebook outline وزر ممتلئ واحد |
| A7 webui | مستودع البرنامج | — | يُدقَّق في الجزء F | — |

## B — النظام الإعلاني التجاري

| البند | المرجع | الحالة | الدليل | ما بقي |
|---|---|---|---|---|
| B0 تثبيت الأرض (أولوية 50) | `app/api/ads/request/route.ts` | **لم يبدأ** | `priority` يُكتب عمودًا في الإدراج (`:158`) بلا قيمة 50 مقصودة | تثبيت أولوية الطلب الذاتي عند 50 |
| B1 الاعتماد بسعر والباقات | `lib/db/schemas/ad-schema.ts` | **جزئي** | لا `approved_price` ولا `plan_id` في المخطط؛ لكن جدول `sponsor_invoices` **موجود** (`lib/content-schema.ts:381`) | أعمدة الاعتماد بسعر، ربط الفاتورة، تبويب الفواتير |
| B2 معالج `/advertise` | `app/advertise/page.tsx` | **لم يبدأ** | صفحة واحدة فقط، لا خطوات ولا `app/advertise/[id]` | المعالج بخمس خطوات |
| B3 لوحة المعلن | — | **لم يبدأ** | لا مجلد `app/advertiser` | اللوحة، `redirect /admin/advertising → /admin/ads` (المجلد القديم غير موجود أصلًا) |

## C — أداة «حدّد أرضك»

| البند | الحالة | الدليل |
|---|---|---|
| صيغة النسخ CSV وقائمة الصيغ | **تم** | `FindMyLand.tsx:374` `COPY_FORMAT_OPTIONS`؛ `src/lib/tools/fml-clipboard.ts`؛ كوميت `48d825b` |
| تصدير DXF/KML/CSV | **تم** | `src/lib/tools/fml-export.ts`، `dxf.ts`؛ كوميت `4ffcc3a` |
| الإدخال اليدوي وتحرير الخلايا | **تم** | كوميت `087a732` |
| الخريطة والقمر الصناعي وأطوال الأضلاع | **تم** | كوميت `d3c0e79` |
| Web Worker للمعالجة | **تم** | `src/workers/fml-preprocess.worker.ts`، `lib/land/ocr/preprocess.ts` |
| الربط بالمنصة (احفظ/أعلن/مسّاحون) | **تم** | ضمن `d3c0e79` |

## D — إشعارات التطبيق المكتبي

| البند | المرجع | الحالة | الدليل | ما بقي |
|---|---|---|---|---|
| API مجمّع | `app/api/admin/office-notifications/route.ts` | **جزئي** | يُرجع `{sponsors, deliveries}` مسطّحة؛ لا `officeName` ولا تجميع `announcements` | التجميع + اسم المكتب |
| `action=resend` / remind | نفسه | **لم يبدأ** | `grep resend` = صفر | إعادة الإرسال |
| التبويبات واللوحة | `app/admin/integration/admin-integration-client.tsx` | **جزئي** | `OfficeNotificationsPanel` موجود ومربوط (`:11`, `:175`) لكن كقسم مثبّت لا تبويبات | التبويبات، `ComposeCard/AnnouncementList/RecipientPicker`، `office-screens.ts` |

## E — بروتوكول المكتب (الجزء A في الويب)

| البند | الحالة | الدليل |
|---|---|---|
| `/api/office/v1/reference` مع ETag | **تم** | المجلد موجود؛ كوميت `103e491` |
| `OFFICE_PROPERTY_DEAL_TYPES` من `property_offer_types` | **تم** | `lib/integration/office-property.ts:42` = 11 كودًا لا `["sale","rent"]`؛ كوميت `0a2bfc1` |
| `/api/office/v1/settings` (GET/PUT/409/SSE) + انعكاس branding | **تم** | كوميت `fda41d9` + الهجرة `0cf610a` |
| إضافة: `offerTypes` في `/api/program/taxonomies` | **تم** | كوميت `e7ca687` |

## F — برنامج المكتب 3.0.x (`AkarApp_SOURCE`)

| البند | الحالة | الدليل |
|---|---|---|
| قائمة «نوع العرض» من المنصة + ترحيل القيم | **تم** | كوميت `4b80c63` |
| الربط بالجهاز + قناة الإعدادات (409/دون اتصال) | **تم** | كوميت `3ea100a` |
| التبويبات التسعة على الإعدادات المشتركة | **تم** | كوميت `9914304` |
| الأمن والإصدار والمانيفست | **تم** | كوميت `e74dc97`؛ grep الأسرار = صفر |
| الخطوط المضمّنة محليًا وحذف Tajawal | **لم يبدأ** | `webui-src/src/assets/fonts/Tajawal-*.woff2` ما زالت تُحمَّل في البناء |
| شريط الحالة 32px بدل الشريط الإعلاني السفلي | **لم يبدأ** | `AdBottom` ما زال في `Shell.tsx` |
| الأخبار داخل هيدر 56px وحذف `.ticker` | **لم يبدأ** | `Ticker.tsx` قائم |
| الرئيسية (4 مؤشرات + «يحتاج انتباهك») | **لم يبدأ** | `DashboardPage.tsx` بلا هذه الأقسام |
| القائمة بخمس مجموعات ومؤشر ذهبي | **لم يبدأ** | `Shell.tsx` قائمة مسطّحة |

---

## ما بقي — مرتّبًا

### أولًا: المظهر البصري والاتساق
1. **403 إيموجي في 127 ملفًا** → `EmptyState` موحّد وأيقونات lucide.
2. **88 موضع نص تحت 12px** → 12–14px بوزن 500–600.
3. **1614 استعمالًا لألوان Tailwind الخام** → توكنز، وتفريغ allowlist.
4. الشعار في `AuthPageShell` و`AccountDialog` و`DashboardPageShell` و`admin-sidebar` (ما زال يرسم «A»).
5. سياسة الإعلان الفارغ الثلاثية و`heroEnabled:false` للعوائل العشر.
6. الهيدر والفوتر والشريط السفلي على الجوال، و`PageHeader`.
7. البطاقات (A4)، لوحة المكتب (A5)، الدخول (A6).

### ثانيًا: الوظيفي
8. تجميع إشعارات المكاتب + `resend` + التبويبات.
9. النظام الإعلاني التجاري كاملًا (أولوية 50، الاعتماد بسعر، المعالج، لوحة المعلن).

### ثالثًا: البنيوي
10. خطوط البرنامج المكتبي محليًا، شريط الحالة، الرئيسية، القائمة.

---

## ملاحظات على البرمبت نفسه

- `/admin/advertising` **غير موجود**، فلا شيء يُعاد توجيهه؛ اللوحة القائمة هي `/admin/ads`.
- `NewsTicker` **ليس** sticky أصلًا — البند مستوفى دون عمل.
- `sponsor_invoices` موجود في المخطط منذ قبل هذه الخطط؛ الناقص هو ربطه بالاعتماد.
